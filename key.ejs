const  sanitizeHTML = require('sanitize-html')

req.body.title = sanitizeHTML (req.body.title.trim(), {alowedTags: [] allowedAttributes: {}})
req.body.body = sanitizeHTML (req.body.title.trim(), {alowedTags: [] allowedAttributes: {}})


